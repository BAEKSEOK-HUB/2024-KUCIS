<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시판 목록</title>
    <link rel="stylesheet" href="/css/home/community.css"> <!-- CSS 스타일 적용 -->
</head>
<body>

    <!-- 헤더: NGDB 로고 -->
    <div class="header">
        <a href="/login" class="logo">NGDB</a> <!-- 로고 클릭 시 로그인 페이지로 이동 -->
    </div>

    <!-- 구분선 -->
    <div class="separator"></div>

    <!-- 게시판 목록 컨테이너 -->
    <div class="container">
        <h2>게시판 목록</h2> <!-- 게시판 제목 -->

        <!-- 게시물 목록 -->
        <div id="postList">
            <% if (posts.length > 0) { %> <!-- 게시물이 있는 경우 -->
                <% posts.forEach(function(post) { %> <!-- 각 게시물을 순회하여 표시 -->
                    <div class="post-item">
                        <!-- 게시물 제목: 클릭 시 해당 게시물 상세 페이지로 이동 -->
                        <div class="post-title"><a href="/post/<%= post.postnum %>"><%= post.title %></a></div>
                        <!-- 게시물 내용 -->
                        <div class="post-content"><%= post.content %></div>
                        <!-- 게시물 메타 정보: 작성자 및 작성 시간 -->
                        <div class="post-meta">작성자: <%= post.id %> | <%= new Date(post.created_at).toLocaleString() %></div>
                    </div>
                <% }) %>
            <% } else { %> <!-- 게시물이 없는 경우 -->
                <div class="no-posts">게시물이 없습니다</div>
            <% } %>
        </div>        

        <!-- 글 작성 버튼 -->
        <div class="form-actions">
            <button onclick="location.href='/board'">글 작성하기</button> <!-- 글 작성 페이지로 이동 -->
        </div>
    </div>

</body>
</html>
